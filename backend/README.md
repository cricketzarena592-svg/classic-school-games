# Backend boundary

ClassicSchoolGames currently has no application server. The games are static HTML, CSS, and JavaScript, and room-based multiplayer connects browsers directly through PeerJS/WebRTC.

Keep future server-side code in this directory. A backend is only needed if the project adds persistent accounts, saved scores, matchmaking, or a server-authoritative game state.